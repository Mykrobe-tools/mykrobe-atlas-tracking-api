version: '3.1'

services:

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_HOST_AUTH_METHOD: trust
    expose:
      - "5432"

  app:
    build: ../../
    command: pytest